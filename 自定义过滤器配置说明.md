# 自定义过滤器配置说明

## 📋 概述

自定义过滤器允许你精确控制哪些文件类型需要备份，有三种模式可选：
- **白名单模式**：只备份指定的文件类型
- **黑名单模式**：排除指定的文件类型，备份其他所有文件
- **无过滤模式**：备份所有文件

## 🎯 使用场景

### 场景 1：只备份特定类型的文件
**需求**：只想备份 `.txt` 和 `.docx` 文件

**GUI 操作**：
1. 打开"备份设置"
2. 点击"添加"或"配置"按钮
3. 选择模式：**白名单（仅备份）**
4. 在"文件扩展名"输入框中输入 `txt`，点击"添加"
5. 再输入 `docx`，点击"添加"
6. 点击"确定"保存

**生成的配置**：
```json
{
  "path": "D:\\Documents",
  "enabled": true,
  "presets": [],
  "filter": {
    "mode": "whitelist",
    "extensions": [".txt", ".docx"]
  }
}
```

### 场景 2：排除特定类型的文件
**需求**：备份所有文件，但排除视频文件

**GUI 操作**：
1. 打开"备份设置"
2. 点击"添加"或"配置"按钮
3. 选择模式：**黑名单（排除）**
4. 添加扩展名：`mp4`, `avi`, `mkv`, `mov`
5. 点击"确定"保存

**生成的配置**：
```json
{
  "path": "D:\\Media",
  "enabled": true,
  "presets": [],
  "filter": {
    "mode": "blacklist",
    "extensions": [".mp4", ".avi", ".mkv", ".mov"]
  }
}
```

### 场景 3：结合预设和自定义过滤器
**需求**：使用代码预设，但额外排除 `.log` 文件

**方法 1 - 使用预设 + 黑名单预设**：
```json
{
  "path": "D:\\Projects",
  "enabled": true,
  "presets": ["code", "common_blacklist"]
}
```

**方法 2 - 使用自定义过滤器**：
```json
{
  "path": "D:\\Projects",
  "enabled": true,
  "presets": [],
  "filter": {
    "mode": "whitelist",
    "extensions": [
      ".py", ".cpp", ".h", ".js", ".ts", ".vue",
      ".json", ".yaml", ".md"
    ]
  }
}
```

## ⚙️ 配置参数详解

### 基本结构
```json
{
  "backup_sources": [
    {
      "path": "要监控的路径",
      "enabled": true,
      "presets": ["预设名称"],
      "filter": {
        "mode": "过滤模式",
        "extensions": ["扩展名列表"]
      }
    }
  ]
}
```

### 参数说明

#### path（必填）
- **类型**：字符串
- **说明**：要监控的文件夹或文件路径
- **示例**：
  - 文件夹：`"D:\\Projects\\MyApp"`
  - 单个文件：`"D:\\Documents\\important.docx"`

#### enabled（必填）
- **类型**：布尔值
- **说明**：是否启用此备份源
- **值**：`true` 或 `false`

#### presets（可选）
- **类型**：字符串数组
- **说明**：应用的预设过滤器列表
- **注意**：如果同时设置了 `presets` 和 `filter`，`filter` 会覆盖预设
- **示例**：
  ```json
  "presets": ["code", "common_blacklist"]
  ```

#### filter（可选）
- **类型**：对象
- **说明**：自定义过滤器配置
- **子参数**：
  - `mode`：过滤模式
  - `extensions`：扩展名列表

##### filter.mode
- **类型**：字符串
- **可选值**：
  - `"whitelist"`：白名单模式，只备份列表中的文件类型
  - `"blacklist"`：黑名单模式，排除列表中的文件类型
  - `"none"`：无过滤，备份所有文件
- **示例**：
  ```json
  "mode": "whitelist"
  ```

##### filter.extensions
- **类型**：字符串数组
- **说明**：文件扩展名列表
- **格式**：可以带点或不带点（程序会自动处理）
- **示例**：
  ```json
  "extensions": [".txt", ".docx", "pdf"]
  ```

## 📝 完整配置示例

### 示例 1：多路径不同过滤策略
```json
{
  "backup_destination_base": "D:\\Backups",
  "strategy": {
    "retention_days": 30,
    "max_versions_per_file": 10,
    "enable_compression": true,
    "compression_level": 6
  },
  "backup_sources": [
    {
      "path": "D:\\Projects\\WebApp",
      "enabled": true,
      "presets": [],
      "filter": {
        "mode": "whitelist",
        "extensions": [".js", ".ts", ".vue", ".css", ".html", ".json"]
      }
    },
    {
      "path": "D:\\Documents",
      "enabled": true,
      "presets": ["documents", "common_blacklist"]
    },
    {
      "path": "D:\\Photos",
      "enabled": true,
      "presets": [],
      "filter": {
        "mode": "blacklist",
        "extensions": [".tmp", ".cache"]
      }
    }
  ]
}
```

### 示例 2：只备份源代码
```json
{
  "backup_destination_base": "E:\\CodeBackup",
  "strategy": {
    "retention_days": 60,
    "max_versions_per_file": 20,
    "enable_compression": true,
    "compression_level": 9
  },
  "backup_sources": [
    {
      "path": "D:\\Projects",
      "enabled": true,
      "presets": [],
      "filter": {
        "mode": "whitelist",
        "extensions": [
          ".py", ".cpp", ".h", ".hpp", ".c",
          ".js", ".ts", ".jsx", ".tsx",
          ".java", ".cs", ".go", ".rs",
          ".php", ".rb", ".lua", ".sh",
          ".json", ".yaml", ".yml", ".toml",
          ".md", ".txt"
        ]
      }
    }
  ]
}
```

### 示例 3：排除大文件和临时文件
```json
{
  "backup_destination_base": "F:\\Backup",
  "strategy": {
    "retention_days": 14,
    "max_versions_per_file": 5,
    "enable_compression": true,
    "compression_level": 3,
    "max_file_size": 10485760
  },
  "backup_sources": [
    {
      "path": "D:\\WorkFiles",
      "enabled": true,
      "presets": [],
      "filter": {
        "mode": "blacklist",
        "extensions": [
          ".tmp", ".temp", ".bak", ".cache",
          ".log", ".swp", ".swo",
          ".mp4", ".avi", ".mkv", ".mov",
          ".zip", ".rar", ".7z", ".tar", ".gz"
        ]
      }
    }
  ]
}
```

## 🔄 预设 vs 自定义过滤器

### 使用预设的优势
- ✅ 配置简单，一行搞定
- ✅ 预定义了常见场景
- ✅ 可以组合多个预设
- ✅ 维护方便，统一管理

**示例**：
```json
"presets": ["code", "common_blacklist"]
```

### 使用自定义过滤器的优势
- ✅ 完全自定义，精确控制
- ✅ 适合特殊需求
- ✅ 不依赖预设文件
- ✅ 配置更直观

**示例**：
```json
"filter": {
  "mode": "whitelist",
  "extensions": [".txt", ".docx"]
}
```

### 优先级规则
1. 如果同时设置了 `presets` 和 `filter`，**`filter` 优先**
2. 如果只设置了 `presets`，使用预设规则
3. 如果只设置了 `filter`，使用自定义规则
4. 如果都没设置，备份所有文件

## ⚠️ 注意事项

### 1. 扩展名格式
- 可以写 `.txt` 或 `txt`，程序会自动处理
- 建议统一使用带点的格式：`.txt`

### 2. 大小写
- 扩展名不区分大小写
- `.TXT` 和 `.txt` 是等价的

### 3. 路径格式
- Windows 路径使用双反斜杠：`D:\\Projects`
- 或使用正斜杠：`D:/Projects`

### 4. 配置生效
- 修改配置后需要**重启监控服务**
- 在 GUI 中：停止监控 → 启动监控

### 5. 性能考虑
- 白名单模式性能最好（只检查指定类型）
- 黑名单模式需要检查所有文件
- 扩展名列表不要太长（建议 < 50 个）

## 🐛 常见问题

### Q1: 设置了自定义过滤器，但文件还是没有备份？
**检查清单**：
1. ✅ 确认 `enabled` 为 `true`
2. ✅ 确认扩展名拼写正确
3. ✅ 确认文件大小没有超过 `max_file_size`
4. ✅ 确认已重启监控服务
5. ✅ 查看日志文件确认是否有错误

### Q2: 白名单和黑名单可以同时使用吗？
**答**：不建议在同一个 `filter` 中混用。如果需要复杂规则：
- 方法 1：使用多个备份源，分别配置
- 方法 2：使用预设组合（如 `code` + `common_blacklist`）

### Q3: 如何备份所有文件？
**方法 1**：不设置 `filter` 和 `presets`
```json
{
  "path": "D:\\AllFiles",
  "enabled": true
}
```

**方法 2**：设置 `mode` 为 `none`
```json
{
  "path": "D:\\AllFiles",
  "enabled": true,
  "filter": {
    "mode": "none",
    "extensions": []
  }
}
```

### Q4: 如何在 GUI 中查看当前配置？
1. 点击托盘图标
2. 选择"备份格式"
3. 滚动到底部查看"当前监控路径配置"

### Q5: 配置文件在哪里？
- 主配置文件：`config.json`（程序运行目录）
- 预设文件：`presets.json`（程序运行目录）
- 示例配置：`备份配置文件/` 文件夹

## 📊 配置模板

### 模板 1：开发项目（推荐）
```json
{
  "path": "D:\\Projects\\MyApp",
  "enabled": true,
  "presets": ["code", "common_blacklist", "build_artifacts"]
}
```

### 模板 2：文档备份
```json
{
  "path": "D:\\Documents",
  "enabled": true,
  "presets": ["documents", "images", "common_blacklist"]
}
```

### 模板 3：纯文本文件
```json
{
  "path": "D:\\Notes",
  "enabled": true,
  "filter": {
    "mode": "whitelist",
    "extensions": [".txt", ".md", ".log"]
  }
}
```

### 模板 4：排除大文件
```json
{
  "path": "D:\\Mixed",
  "enabled": true,
  "filter": {
    "mode": "blacklist",
    "extensions": [".mp4", ".avi", ".iso", ".zip", ".rar"]
  }
}
```

## 🔧 高级技巧

### 技巧 1：分层备份策略
为不同类型的文件设置不同的备份策略：

```json
{
  "backup_sources": [
    {
      "path": "D:\\Projects\\src",
      "enabled": true,
      "filter": {
        "mode": "whitelist",
        "extensions": [".cpp", ".h", ".py", ".js"]
      }
    },
    {
      "path": "D:\\Projects\\docs",
      "enabled": true,
      "filter": {
        "mode": "whitelist",
        "extensions": [".md", ".txt", ".pdf"]
      }
    }
  ]
}
```

### 技巧 2：临时禁用某个路径
不删除配置，只是暂时不备份：

```json
{
  "path": "D:\\TempProject",
  "enabled": false,
  "presets": ["code"]
}
```

### 技巧 3：监控单个重要文件
```json
{
  "path": "D:\\Important\\database.db",
  "enabled": true,
  "filter": {
    "mode": "none"
  }
}
```

## 📞 获取帮助

- 查看日志：`备份目录/日期/backup_log_日期.log`
- 配置示例：`备份配置文件/` 文件夹
- 主文档：`README.md`
- 功能说明：`FEATURES_V3.md`

## 🎓 学习路径

1. **入门**：使用预设配置（`presets`）
2. **进阶**：组合多个预设
3. **高级**：使用自定义过滤器（`filter`）
4. **专家**：混合使用预设和自定义过滤器

---

**提示**：建议先在测试目录上试验配置，确认无误后再应用到重要数据！
