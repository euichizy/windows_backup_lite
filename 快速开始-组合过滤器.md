# 快速开始 - 组合过滤器

## 🚀 5分钟上手

### 场景 1：Python 项目

**需求**：备份 Python 代码，排除编译文件和日志

**配置**：
```json
{
  "path": "D:\\Projects\\PythonApp",
  "enabled": true,
  "presets": ["code", "common_blacklist"],
  "filter": {
    "blacklist": [".pyc", ".pyo", ".log"]
  }
}
```

**效果**：
- ✅ 备份：.py, .json, .yaml, .md 等代码文件
- ❌ 排除：.pyc, .pyo, .log, .tmp, .cache 等

---

### 场景 2：前端项目

**需求**：只备份源代码，不要压缩文件和 node_modules

**配置**：
```json
{
  "path": "D:\\Projects\\WebApp",
  "enabled": true,
  "presets": ["common_blacklist"],
  "filter": {
    "whitelist": [".js", ".ts", ".vue", ".html", ".css", ".json"],
    "blacklist": [".min.js", ".min.css"]
  }
}
```

**效果**：
- ✅ 备份：.js, .ts, .vue, .html, .css, .json
- ❌ 排除：.min.js, .min.css, node_modules, .cache 等

---

### 场景 3：文档项目

**需求**：备份文档和图片，排除临时文件

**配置**：
```json
{
  "path": "D:\\Documents",
  "enabled": true,
  "presets": ["documents", "images", "common_blacklist"]
}
```

**效果**：
- ✅ 备份：.docx, .pdf, .txt, .jpg, .png 等
- ❌ 排除：.tmp, .bak, .cache 等

---

## 📋 配置模板

### 模板 1：代码项目（推荐）

```json
{
  "path": "你的项目路径",
  "enabled": true,
  "presets": ["code", "common_blacklist"],
  "filter": {
    "blacklist": ["额外要排除的扩展名"]
  }
}
```

### 模板 2：精确控制

```json
{
  "path": "你的项目路径",
  "enabled": true,
  "presets": [],
  "filter": {
    "whitelist": ["要备份的扩展名"],
    "blacklist": ["要排除的扩展名"]
  }
}
```

### 模板 3：只用预设

```json
{
  "path": "你的项目路径",
  "enabled": true,
  "presets": ["code", "common_blacklist"]
}
```

---

## 🎯 常用组合

### Python 项目
```json
"presets": ["code", "common_blacklist"],
"filter": {
  "blacklist": [".pyc", ".pyo", "__pycache__"]
}
```

### JavaScript/Node.js 项目
```json
"presets": ["code", "common_blacklist"],
"filter": {
  "blacklist": ["node_modules", ".next", "dist"]
}
```

### C++ 项目
```json
"presets": ["code", "common_blacklist", "build_artifacts"]
```

### 文档项目
```json
"presets": ["documents", "images", "common_blacklist"]
```

---

## ⚡ 快速测试

### 步骤 1：创建测试配置

编辑 `config.json`：
```json
{
  "backup_destination_base": "D:\\TestBackup",
  "strategy": {
    "retention_days": 7,
    "max_versions_per_file": 5,
    "enable_compression": true,
    "compression_level": 3
  },
  "backup_sources": [
    {
      "path": "D:\\TestProject",
      "enabled": true,
      "presets": ["code"],
      "filter": {
        "blacklist": [".log"]
      }
    }
  ]
}
```

### 步骤 2：创建测试文件

在 `D:\TestProject` 创建：
- `test.py` ✅ 应该备份
- `test.js` ✅ 应该备份
- `test.log` ❌ 不应该备份
- `test.txt` ❌ 不应该备份（不在code预设中）

### 步骤 3：启动监控

1. 启动程序
2. 右键托盘图标 → "启动监控"
3. 修改测试文件
4. 检查 `D:\TestBackup` 目录

### 步骤 4：验证结果

查看备份目录，应该只有：
- `test.py` 的备份
- `test.js` 的备份

查看日志文件确认：
```
D:\TestBackup\2024-XX-XX\backup_log_*.log
```

---

## 💡 使用技巧

### 技巧 1：从简单开始

先用预设：
```json
"presets": ["code", "common_blacklist"]
```

需要时再添加自定义：
```json
"presets": ["code", "common_blacklist"],
"filter": {
  "blacklist": [".pyc"]
}
```

### 技巧 2：查看预设内容

打开 `presets.json` 查看可用的预设和它们包含的文件类型。

### 技巧 3：测试配置

在小目录上测试配置，确认无误后再应用到大项目。

### 技巧 4：查看日志

遇到问题时，查看日志文件了解哪些文件被跳过了。

---

## 🐛 常见问题

### Q: 文件没有备份？

**检查清单**：
1. ✅ 确认文件扩展名在白名单中（如果有白名单）
2. ✅ 确认文件扩展名不在黑名单中
3. ✅ 确认监控服务已启动
4. ✅ 查看日志文件

### Q: 备份了不想要的文件？

**解决方案**：
添加到黑名单：
```json
"filter": {
  "blacklist": [".unwanted"]
}
```

### Q: 如何查看最终的过滤规则？

**方法**：
1. 右键托盘图标 → "备份格式"
2. 滚动到底部查看"当前监控路径配置"
3. 或查看日志文件

---

## 📞 获取帮助

### 文档

- **详细说明**：`预设与自定义过滤器组合使用说明.md`
- **配置参考**：`快速配置参考.md`
- **完整文档**：`备份配置文件/README.md`

### 日志

- **位置**：`备份目录/日期/backup_log_*.log`
- **查看**：用记事本打开

### 配置文件

- **主配置**：`config.json`
- **预设**：`presets.json`
- **示例**：`备份配置文件/config_自定义过滤器示例.json`

---

## 🎉 开始使用

1. 复制上面的配置模板
2. 修改路径和扩展名
3. 保存到 `config.json`
4. 启动监控
5. 测试验证

**就这么简单！** 🚀
